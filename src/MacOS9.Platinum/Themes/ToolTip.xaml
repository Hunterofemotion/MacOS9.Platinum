<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/MacOS9.Platinum;component/Themes/Palette.xaml" />
        <ResourceDictionary Source="/MacOS9.Platinum;component/Themes/Typography.xaml" />
        <ResourceDictionary Source="/MacOS9.Platinum;component/Themes/PopupChrome.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Etiqueta emergente: la nota amarilla de la ayuda de Mac OS 9, con filo negro -->
    <!-- y la sombra proyectada estándar de 2 px. La sombra propia sustituye a la del -->
    <!-- sistema, que va difuminada y desentona con el tema.                          -->
    <Style x:Key="PlatinumToolTip" TargetType="{x:Type ToolTip}">
        <Setter Property="FontFamily" Value="{StaticResource ViewFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource SmallFontSize}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="HasDropShadow" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToolTip}">
                    <ContentControl Template="{StaticResource PlatinumPopupChrome}"
                                    Background="{StaticResource TipFaceBrush}"
                                    Padding="6,3"
                                    IsTabStop="False"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ToolTip}" BasedOn="{StaticResource PlatinumToolTip}" />

</ResourceDictionary>
