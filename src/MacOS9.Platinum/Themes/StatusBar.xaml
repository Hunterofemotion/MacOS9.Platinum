<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/MacOS9.Platinum;component/Themes/Palette.xaml" />
        <ResourceDictionary Source="/MacOS9.Platinum;component/Themes/Typography.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Barra de estado del borde inferior de la ventana. En Mac OS 9 no es un      -->
    <!-- control hundido ni levantado: es la misma cara de la ventana separada del   -->
    <!-- contenido por una línea grabada de dos tonos, la sombra arriba y el realce  -->
    <!-- abajo. Se apoya en el StatusBar de WPF en vez de inventar un control: así   -->
    <!-- los StatusBarItem, los separadores y la accesibilidad ya vienen resueltos.  -->
    <Style x:Key="PlatinumStatusBar" TargetType="{x:Type StatusBar}">
        <Setter Property="Background" Value="{StaticResource WindowFaceBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource SystemFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource SystemFontSize}" />
        <Setter Property="MinHeight" Value="22" />
        <Setter Property="Padding" Value="8,0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type StatusBar}">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- La línea grabada: un píxel de sombra y otro de realce. -->
                        <Border Grid.Row="0" Height="1"
                                Background="{StaticResource BevelShadowBrush}" />
                        <Border Grid.Row="0" Height="1" Margin="0,1,0,0"
                                Background="{StaticResource BevelHighlightBrush}" />

                        <ItemsPresenter Grid.Row="1"
                                        Margin="{TemplateBinding Padding}"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <!-- Acoplado y no apilado: las celdas fijas se anclan a un lado
                         con DockPanel.Dock y la última se queda con el ancho que
                         sobre, que es como se reparte una barra de estado. -->
                    <DockPanel LastChildFill="True" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PlatinumStatusBarItem" TargetType="{x:Type StatusBarItem}">
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource SystemFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource SystemFontSize}" />
        <Setter Property="Padding" Value="0,2" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type StatusBarItem}">
                    <ContentPresenter Margin="{TemplateBinding Padding}"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type StatusBar}" BasedOn="{StaticResource PlatinumStatusBar}" />
    <Style TargetType="{x:Type StatusBarItem}" BasedOn="{StaticResource PlatinumStatusBarItem}" />

</ResourceDictionary>
