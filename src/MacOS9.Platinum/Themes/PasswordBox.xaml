<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/MacOS9.Platinum;component/Themes/Palette.xaml" />
        <ResourceDictionary Source="/MacOS9.Platinum;component/Themes/Typography.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Campo de contraseña. Mismo pozo hundido que el TextBox: marco negro, sombra  -->
    <!-- arriba e izquierda, realce abajo y derecha. Va en archivo aparte y no en     -->
    <!-- TextBox.xaml porque PasswordBox no hereda de TextBoxBase y no comparte ni    -->
    <!-- estilo ni plantilla con él, solo el aspecto.                                 -->
    <!--                                                                              -->
    <!-- La tipografía es la del contenido y no la del chrome: lo que se escribe aquí -->
    <!-- son datos del usuario, aunque salgan enmascarados.                           -->
    <Style x:Key="PlatinumPasswordBox" TargetType="{x:Type PasswordBox}">
        <Setter Property="FontFamily" Value="{StaticResource ViewFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource ViewFontSize}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="CaretBrush" Value="{StaticResource TextBrush}" />
        <Setter Property="Padding" Value="3,1" />
        <Setter Property="MinHeight" Value="22" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">
                    <Border x:Name="Frame"
                            BorderThickness="1"
                            BorderBrush="{StaticResource TextBrush}"
                            Background="{StaticResource ContentFaceBrush}">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden" />
                            <Border BorderThickness="0,0,1,1"
                                    BorderBrush="{StaticResource BevelHighlightBrush}" />
                            <Border BorderThickness="1,1,0,0"
                                    BorderBrush="{StaticResource BevelShadowBrush}" />
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!-- Deshabilitado pierde el marco negro y el texto baja de -->
                        <!-- intensidad, igual que el campo de texto.               -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Frame" Property="BorderBrush"
                                    Value="{StaticResource BevelShadowBrush}" />
                            <Setter Property="Foreground"
                                    Value="{StaticResource TextDisabledBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type PasswordBox}" BasedOn="{StaticResource PlatinumPasswordBox}" />

</ResourceDictionary>
