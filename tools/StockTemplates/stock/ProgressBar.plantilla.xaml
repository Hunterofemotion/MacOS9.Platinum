<ControlTemplate TargetType="ProgressBar" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:s="clr-namespace:System;assembly=System.Private.CoreLib">
  <Grid Name="TemplateRoot">
    <VisualStateManager.VisualStateGroups>
      <VisualStateGroup Name="CommonStates" />
    </VisualStateManager.VisualStateGroups>
    <Border BorderThickness="{TemplateBinding Border.BorderThickness}" BorderBrush="{TemplateBinding Border.BorderBrush}" Background="{TemplateBinding Panel.Background}" />
    <Rectangle Name="PART_Track" />
    <Grid Name="PART_Indicator" HorizontalAlignment="Left" ClipToBounds="True">
      <Rectangle Fill="{TemplateBinding TextElement.Foreground}" Name="Indicator" />
      <Rectangle Fill="{TemplateBinding TextElement.Foreground}" Name="Animation" RenderTransformOrigin="0.5,0.5">
        <Rectangle.RenderTransform>
          <TransformGroup>
            <TransformGroup.Children>
              <ScaleTransform />
              <SkewTransform />
              <RotateTransform />
              <TranslateTransform />
            </TransformGroup.Children>
          </TransformGroup>
        </Rectangle.RenderTransform>
      </Rectangle>
    </Grid>
  </Grid>
  <ControlTemplate.Triggers>
    <Trigger Property="ProgressBar.Orientation">
      <Setter Property="FrameworkElement.LayoutTransform" TargetName="TemplateRoot">
        <Setter.Value>
          <RotateTransform Angle="-90" />
        </Setter.Value>
      </Setter>
      <Trigger.Value>
        <x:Static Member="Orientation.Vertical" />
      </Trigger.Value>
    </Trigger>
    <Trigger Property="ProgressBar.IsIndeterminate">
      <Setter Property="UIElement.Visibility" TargetName="Indicator">
        <Setter.Value>
          <x:Static Member="Visibility.Collapsed" />
        </Setter.Value>
      </Setter>
      <Trigger.Value>
        <s:Boolean>True</s:Boolean>
      </Trigger.Value>
    </Trigger>
  </ControlTemplate.Triggers>
</ControlTemplate>